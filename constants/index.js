const { contractAddress, marketplaceAddress } = require("./contractAddress")
const abi = require("./abi.json")
const marketplaceAbi = require("./marketplaceAbi.json")

module.exports = {
    contractAddress, abi, marketplaceAddress, marketplaceAbi
}